{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}

{% block body %}
<style type="text/css">
    div.left{
        text-align: right;
    }
</style>
 <!-- Vertical navbar -->
  <div class="vertical-nav bg-dark active" id="sidebar">
      <div class="py-4 px-3 mb-4 bg-dark">
          <div class="media-body">
            <h4 class="font-weight-white text-muted mb-0">Show Project</h4>
          </div>
      </div>
      <ul class="nav flex-column bg-dark mb-0">
        <li class="nav-item">
            <a href="{{ path('app_dashboard') }}" class="nav-link text-light font-italic bg-dark">
                  <i class="fa fa-home mr-3 text-primary fa-fw" aria-hidden="true"></i>
                    Dashboard
              </a>
        </li>
         <p class="text-gray font-weight-bold text-uppercase px-3 small py-4 mb-0">Project</p>
        <li class="nav-item">
            <a href="{{ path('project_new') }}"  class="nav-link text-light font-italic bg-dark">
                  <i class="fa fa-plus-square-o mr-3 text-primary fa-fw" aria-hidden="true"></i>
                  Create New Project
              </a>
        </li>
        <li class="nav-item">
            <a href="{{ path('project_index') }}" class="nav-link text-light font-italic bg-dark">
                  <i class="fa fa-cubes mr-3 text-primary fa-fw"></i>
                 Project Index
              </a>
        </li>
         <li class="nav-item">
            <a href="{{ path('project_edit', {'id': project.id}) }}"  class="nav-link text-light font-italic bg-dark">
                  <i class="fa fa-cubes mr-3 text-primary fa-fw"></i>
                Edit Project
              </a>
        </li>
      <p class="text-gray font-weight-bold text-uppercase px-3 small py-4 mb-0">Deconnexion</p>
          <li class="nav-item">
              <a href="{{path('app_logout') }}"  class="nav-link text-light font-italic bg-dark">
                  <i class="fa fa-sign-out text-primary mr-3 fa-fw" aria-hidden="true"></i>
                    Logout
                </a>
          </li>
        </ul>
  </div>
<!-- End vertical navbar -->

<center><div class="box">
    <div class="left">     <a href="{{ path('project_edit', {'id': project.id}) }}">Edit</a><br>
   <a href="{{ path('project_index') }}">Back to list</a>
   </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Name_project</th>
                <td>{{ project.nameProject }}</td>
            </tr>
               <tr>
                <th>user</th>
            <td>{% for u in project.user %}
            {{ u }}<br>
              {% endfor %}</td>

               </tr>
        </tbody>
    </table>

 

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}
