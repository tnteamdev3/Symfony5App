

{% block body %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="button" class="btn btn-primary btn-lg">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

{% endblock %}